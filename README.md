# threads-auto-assets

[@broken.ai.log](https://www.threads.com/@broken.ai.log) 카드 이미지 호스팅용.

스레드 API는 이미지를 공개 URL로만 받는다. 본체 저장소는 비공개라 여기만 따로 열었다.
**여기 들어오는 것은 발행 예정인 카드 PNG뿐이다** — 토큰·기록·대기열은 본체에 있다.
원본은 본체의 대기열 글이고, 이 PNG들은 `scripts/카드.py`가 언제든 다시 만든다.
